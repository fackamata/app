{% extends 'admin/admin.html.twig' %}


{% block title %}Conseil index{% endblock %}

{% block body %}
    <h1>Conseil index</h1>

    <table class="table" id="myTable">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Description</th>
                <th>DatePublication</th>
                <th>NombreVue</th>
                <th>Photo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for conseil in conseils %}
            <tr>
                <td>{{ conseil.id }}</td>
                <td>{{ conseil.titre }}</td>
                <td>{{ conseil.description }}</td>
                <td>{{ conseil.datePublication ? conseil.datePublication|date('d-m-Y H:i') : '' }}</td>
                <td>{{ conseil.nombreVue }}</td>
                <td>
                    {% if conseil.photo %}
                         <img src="{{ conseil.photo }}" alt="{{ conseil.titre }}" class="img-fluid" />
                    {% endif %}
                </td>
                <td>
                    <a class="btn btn-success" href="{{ path('conseil_show', {'id': conseil.id}) }}">show</a>
                    <a class="btn btn-warning" href="{{ path('conseil_edit', {'id': conseil.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('conseil_new') }}">Create new</a>
{% endblock %}
