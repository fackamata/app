{% extends 'admin/admin.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
<div>
    <h1>Liste des utilisateurs</h1>

    <div class="table-responsive">
        <table class="table table-hover" id="myTable">
            <thead>
                <tr>
                    <th>Username</th>
                    <th>Roles</th>
                    <!-- <th>Password</th> -->
                    <th>Nom</th>
                    <th>Prenom</th>
                    <th>Mail</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.username }}</td>
                    <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                <!--  <td>{{ user.password }}</td> -->
                    <td>{{ user.nom }}</td>
                    <td>{{ user.prenom }}</td>
                    <td>{{ user.mail }}</td>
                    <td>
                        <a class="btn btn-outline-info" href="{{ path('admin_user_show', {'id': user.id}) }}">Détail</a>
                        <a class="btn btn-outline-warning" href="{{ path('admin_user_edit', {'id': user.id}) }}">Modifier</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="9">Aucun utilisateur</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <a class="btn btn-outline-success" href="{{ path('admin_user_new') }}">Créer un nouveau utilisateur</a>
</div>
{% endblock %}
