{% extends 'base.html.twig' %}

{% block title %}Les conseils{% endblock %}

{% block body %}
    <h1>Les conseils</h1>
    <div class="d-flex flex-wrap ">


    {% for conseil in conseils %}

    <div class="card m-2" style="width: 18rem;">
        {% if conseil.photo %}
            <img src="{{ conseil.photo }}" class="card-img-top" alt="{{ conseil.titre }}" class="img-fluid" />
        {% endif %}
        <div class="card-body">
          <h5 class="card-title">{{ conseil.titre }}</h5>
          <div class="card-text">
            <div class="d-flex justify-content-between">
                <p>{{ conseil.user }}</p>
                <p>{{ conseil.nombreVue }} vues</p>
            </div>
        </div>
          <a class="btn btn-info" href="{{ path('conseil_show', {'id': conseil.id}) }}">Détail</a>
        </div>
      </div>
    
    {% else %}
        <div>
            <p>Aucun conseil</p>
        </div>
    {% endfor %}  

    

</div>
<a class="btn btn-info" href="javascript:history.back()">retour</a>
    {% if is_granted('ROLE_USER') or is_granted('ROLE_ADMIN') %}
		<a class="btn btn-info" href="{{ path('conseil_new') }}">Créer un conseil</a>
	{% endif %}

{% endblock %}
