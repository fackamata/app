{% extends 'base.html.twig' %}

{% block title %}Modification commentaire{% endblock %}

{% block body %}
    <h1>Modification commentaire</h1>
    <div class="form-SAMM d-flex justify-content-between">
        <a class="btn btn-outline-info " href="javascript:history.back()">Retour</a>
        <form method="post" action="{{ path('avis_delete', {'id': avi.id}) }}" onsubmit="return confirm('Etes-vous sûre de vouloir supprimer ce commentaire ?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avi.id) }}">
            <button class="btn btn-outline-danger">{{ button_label|default('Delete') }}</button>
        </form>
    </div>
    <div class="form-SAMM">{{form(form)}}</div>


{% endblock %}
