{% extends 'base.html.twig' %}

{% block title %}Modifier Annonce{% endblock %}

{% block body %}
    <h1>Modifier Annonce</h1>   

    <div class="form-SAMM">
        <div class="d-flex justify-content-between mb-2">
            <a class="btn btn-outline-info me-2" href="javascript:history.back()">Retour</a>
            <form method="post" action="{{ path('annonce_delete', {'id': annonce.id}) }}" onsubmit="return confirm('Etes-vous sûre de vouloir supprimer cette annonce ?');">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annonce.id) }}">
                <button class="btn btn-outline-danger">Suprimer</button>
            </form>
        </div>  
        {{form(form)}}
    </div>
{% endblock %}

{% block javascripts %}
	<script src="https://cdn.tiny.cloud/1/mmtelnjo7hq8l63uso5nqk221f0r1rumvtukus48ibaqs46v/tinymce/5/tinymce.min.js" referrerpolicy="origin"></script>

    <script>
        $(document).ready( function () {
                tinymce.init({
            selector: 'textarea.tinymce',
            plugins: 'advlist autolink lists link',
            toolbar_mode: 'default',
            });

        } );  
    </script>
{% endblock %}
