{% extends 'base.html.twig' %}

{% block title %}Message{% endblock %}

{% block body %}
    <h1>Annonce : {{message.annonce}}</h1>
    {# {{dump(annonce)}}
    {{dump(sender)}}
    {{dump(destinataire)}} #}
    <div class="form-SAMM">  
        <a class="btn btn-outline-info " href="javascript:history.back()">Retour</a>
        <div class=''>
            <h2>Message de : {{message.sender}}</h2>
    
            <p>Envoyer le : {{ message.date ? message.date|date('d/m/Y H:i:s') : '' }}</p>
    
            <p>{{ message.contenu }}</p>
    
                {# comme c'est une réponse, on inverse le sender et le destinataire(annoneUser) #}
            <a class="btn btn-outline-info" 
            href="{{ path('message_new_reponse', {'idAnnonce': annonce.id, 'idAnnonceUser': sender.id, 'idMessageSender': destinataire.id }) }}">Répondre</a>
        </div>

    </div>
    {# {{ include('message/_delete_form.html.twig') }} #}
{% endblock %}
